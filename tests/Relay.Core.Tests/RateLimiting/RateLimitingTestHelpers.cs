namespace Relay.Core.Tests.RateLimiting;

public static class RateLimitingTestHelpers
{
    // Helper classes and utilities for rate limiting tests
    // Currently empty as the original tests didn't have specific helper classes
}